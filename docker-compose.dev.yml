version: '3.8'
services:
  humanonn-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8501:8501"
    volumes:
      - ./:/app:cached
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1